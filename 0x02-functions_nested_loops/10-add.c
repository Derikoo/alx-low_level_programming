#include "main.h"

/**
 * add - adds two integers and returns the result
 * @f: the first integer to compute
 * @d: the second integer to compute
 * Return: integer value
 */

int add(int f, int d)
{
	return (f + d);
}
