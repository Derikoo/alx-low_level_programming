#include "main.h"

/**
 * mul - multiplies two integers
 * @f: The first number to multiply
 * @q: The second number to multiply
 * Return: The result of the multiplication between x param and y param
 */

int mul(int f, int q)
{
	return (f * q);
}
